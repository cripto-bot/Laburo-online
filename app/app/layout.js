// Este es el layout raíz y es requerido.
// Puedes personalizarlo más adelante.
export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
